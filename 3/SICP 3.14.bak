#lang racket

(require rnrs/mutable-pairs-6)
(require compatibility/mlist)

(define (reverse x)
  (define (loop x y)
    (if (null? x)
        y
        (let ((temp (cdr x)))
          (set-cdr! x y)
          (loop temp x))))
  (loop x '())
  )

